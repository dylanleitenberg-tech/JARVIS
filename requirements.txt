aiohttp>=3.9,<4
numpy>=1.24,<2
opencv-python>=4.9,<5
mediapipe==0.10.21
psutil>=5.9
anthropic>=0.40
httpx>=0.27
# macOS: Quartz for events and windows, AppKit for media keys and the pasteboard
pyobjc-framework-Quartz>=10.0; sys_platform == "darwin"
pyobjc-framework-Cocoa>=10.0; sys_platform == "darwin"
# Windows and Linux: keyboard, mouse and screen
pyautogui>=0.9.54; sys_platform != "darwin"
pillow>=10; sys_platform != "darwin"
# Windows: exact volume control (without it volume moves in key presses)
pycaw>=20240210; sys_platform == "win32"
